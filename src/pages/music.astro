---
import BaseLayout from '@/layouts/BaseLayout.astro';

const musicItems = [
  { title: 'Favorite Albums', gradient: 'from-neon-purple to-neon-pink' },
  { title: 'Playlists', gradient: 'from-neon-blue to-neon-purple' },
  { title: 'Concert Memories', gradient: 'from-neon-pink to-silver' },
  { title: 'Musical Influences', gradient: 'from-silver to-neon-blue' },
];
---

<BaseLayout 
  title="Music - Nikos Dimitriadis"
  description="My musical journey, favorite albums, and concert experiences"
>
  <!-- Header -->
  <section class="py-20 px-4">
    <div class="max-w-4xl mx-auto text-center">
      <h1 class="text-4xl md:text-6xl font-bold font-heading mb-6 animate-fade-in">
        Music & Sound
      </h1>
      <p class="text-xl text-gray-400 max-w-2xl mx-auto animate-fade-in" style="animation-delay: 100ms">
        Music has been a constant companion through life's journey. From the energy of live concerts to the solitude of discovering new artists, it shapes how I experience the world.
      </p>
    </div>
  </section>

  <!-- Music Grid -->
  <section class="py-12 px-4">
    <div class="max-w-6xl mx-auto">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
        {musicItems.map((item, index) => (
          <div 
            class={`bg-gradient-to-br ${item.gradient} p-8 rounded-2xl border border-gray-700 bg-white/5 hover:scale-105 transition-all duration-300 cursor-pointer fade-on-scroll group`}
            style={`animation-delay: ${index * 100}ms`}
          >
            <div class="aspect-square flex items-center justify-center">
              <h3 class="text-2xl font-bold font-heading text-white text-center group-hover:scale-110 transition-transform duration-200">
                {item.title}
              </h3>
            </div>
            <p class="text-sm text-gray-300 text-center mt-4 opacity-0 group-hover:opacity-100 transition-opacity duration-200">
              [Coming soon]
            </p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Future Content Section -->
  <section class="py-20 px-4">
    <div class="max-w-4xl mx-auto text-center">
      <div class="bg-medium-gray border border-gray-800 rounded-2xl p-12">
        <h2 class="text-3xl font-bold font-heading mb-4">More Coming Soon</h2>
        <p class="text-gray-400 max-w-2xl mx-auto">
          I'm currently curating content about my musical journey, favorite albums, concert experiences, and the artists that have shaped my taste. Check back soon for updates!
        </p>
      </div>
    </div>
  </section>
</BaseLayout>
